<?php

namespace app\index\model;

use think\Model;
use think\Db;

class Lists extends Model
{	
	public function ListsInfo()
	{
		//$id = input('param.id');
		//dump($id);
		/*$info = Db::Table('shop_goods')
			->field('shop_goods.goodsname','shop_goods.attrbutes')
			->view('shop_category','uname','shop_goods.s_sid = shop_category.sid')
			->select();
		return $info;*/
	}
	
}